/**
 * State enum used to represent
 * the three possible square states.
 * An enum sets a number to correspond with
 * each state (CLOSED = 0, OPEN = 1, FLAGGED = 2).
 * 
 * @author Jeremy Hui
 */
public enum State {
    CLOSED,
    OPEN,
    FLAGGED
}
